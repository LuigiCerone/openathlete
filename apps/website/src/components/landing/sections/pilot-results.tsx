/**
 * Former “metrics” strip removed: it contained unverified statistics.
 * This section is kept as a no-op so imports stay stable during the redesign.
 */
export function PilotResults() {
  return null;
}
